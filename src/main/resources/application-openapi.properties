spring.application.name=proxy

# Disable OAuth2 resource server for spec generation
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.oauth2.resource.server.servlet.OAuth2ResourceServerAutoConfiguration
